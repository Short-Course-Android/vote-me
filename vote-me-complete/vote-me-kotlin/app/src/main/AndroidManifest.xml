<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="io.ugshortcourse.voteme"
          android:installLocation="auto">
    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
            android:name=".core.VoteMeApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/VoteMe.Theme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name=".view.HomeActivity"
                android:label="@string/title_activity_home">
        </activity>
        <!-- SERVICES -->
        <service android:name=".core.services.VoteMeFirebaseMessaging">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <meta-data
                android:name="com.google.firebase.messaging.default_notification_icon"
                android:resource="@mipmap/ic_launcher_round"/>
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_color"
                android:resource="@color/colorAccent"/>
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_channel_id"
                android:value="@string/default_notification_channel_id"/> <!-- ACTIVITIES -->
        <activity
                android:name=".view.MainActivity"
                android:theme="@style/VoteMe.Theme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.LoginActivity"/>
        <activity android:name=".view.CategoryActivity"/>
        <activity android:name=".view.VotingActivity"/>
        <activity android:name=".view.fragments.VoterInfoFragment"/>
    </application>

</manifest>